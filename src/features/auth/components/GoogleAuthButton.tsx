// GoogleAuthButton — DISABLED.
//
// Google OAuth will be handled by the BI Identity Service in the future.
// For now, users login at /id/login with email/password.
// These components are kept as no-ops for backward compatibility but render nothing.

export function GoogleAuthButton() {
  return null;
}

export function GoogleAuthButtonStyled() {
  return null;
}
